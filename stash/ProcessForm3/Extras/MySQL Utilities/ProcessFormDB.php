<?php// enter these server-specific settings...$mysql_database = "your_database";		// name of mysql database to connect to$mysql_hostname = "localhost";			// server or host name for mysql database ("localhost" usually)$mysql_user = "your_username";			// user name for mysql database (must have "INSERT" permissions)$mysql_password = "your_password";		// password for mysql database$mysql_table = "your_table_name";		// database table name// test the connection and report any errors to script...@mysql_connect($mysql_hostname, $mysql_user, $mysql_password);@$db_selection = mysql_select_db($mysql_database);if (!$db_selection) $errors .= $mysqlErrMsg1."<br>";@$db_encode = mysql_set_charset('utf8');// for better security, place this file in a cgi-bin or other directory above your web root.?>